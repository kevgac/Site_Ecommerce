{% extends 'base-admin.html.twig' %}

{% block title %}E-com - Admin{% endblock %}

{% block body %}

<div class="container">
    <h1 class="mt-5">Dashboard Administrateur</h1>
    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    Produits
                </div>
                <div class="card-body">
                    <a href="{{ path('app_product_index')}}" class="btn btn-success">Gérer les produits</a>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    Utilisateurs
                </div>
                <div class="card-body">
                    <a href="{{ path('app_user_index')}}" class="btn btn-success">Gérer les utilisateurs</a>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
