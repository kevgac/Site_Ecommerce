{% extends 'base.html.twig' %}

{% block title %}Détails du produit{{ product.name }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ product.name }}</h1>
        <p>Description : </p>
        <p>Prix : {{ product.priceHT }}€</p>
        <img src="{{ asset(product.image.path) }}" alt="{{ product.name }}" class="img-fluid">
    </div>
{% endblock %}
